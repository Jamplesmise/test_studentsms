# from studentssms.models import Student, Course
# print(Student._meta.get_fields())
# print([field.name for field in Student._meta.get_fields()])
# print({field.name: field.get_internal_type() for field in Student._meta.get_fields()})
# students = Student.objects.all()
# print(students)
# student = Student.objects.get(pk=1)  # 用实际的主键值代替 1
# print(student)
# student = Student.objects.get(pk=1)  # 用实际的主键值代替 1
# courses = student.courses.all()
# print(courses)
